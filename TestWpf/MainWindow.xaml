<Window x:Class="TestWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Vertical">
            <TextBlock Name="wifi" Text="{Binding wifi}" ></TextBlock>
            <TextBlock Name="wifi1" Text="{Binding wifi1}"></TextBlock>
            <TextBlock Name="wifi2" Text="{Binding wifi2}"></TextBlock>
            <TextBlock Name="wifi3" Text="{Binding wifi3}"></TextBlock>
            <TextBlock Name="wifi4" Text="{Binding wifi4}"></TextBlock>
        </StackPanel>

    </Grid>
</Window>
